def heapsort(array)
  heap = make_into_heap(array)

end

def make_into_heap(array)
end

# inserts data into max heap
# swaps head (largest) into last value
# removes last value and inserts it into new array
# resorts heap 
